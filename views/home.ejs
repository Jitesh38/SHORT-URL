<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener By Jitesh</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
    </style>
</head>

<body>
    <h1>URL Shortner</h1>
    <form action="/url" method="post">
        <label for="url">Enter Url</label>
        <input type="text" name="url" id="">
        <input type="submit" value="Submit">
    </form>
    <% if (locals.id) { %>
        <a href="http://localhost:8001/<%= locals.id %>" target="_blank">
            http://localhost:8001/<%= locals.id %>

        </a>
        <% } %>

            <% if (locals.allurls) { %>
                <table>
                    <thead>
                        <th>Index</th>
                        <th>Short URL</th>
                        <th>Redirected URL</th>
                        <th>Total Visits</th>
                    </thead>
                    <tbody>
                        <% allurls.forEach(url=> { %>
                            <tr>
                                <td>1</td>
                                <td>
                                    <%= url.shortId %>
                                </td>
                                <td>
                                    <%= url.redirectUrl %>
                                </td>
                                <td>
                                    <%= url.visitHistory.length %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
                <% } %>

</body>

</html>